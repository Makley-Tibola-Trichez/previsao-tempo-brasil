import { StyleSheet } from "react-native";

const textStyles = StyleSheet.create({
  bold: {
    fontWeight: "bold",
  },
  normal: {},
  title: {
    fontSize: 20,
    fontWeight: "bold",
  },
});

export default textStyles;
