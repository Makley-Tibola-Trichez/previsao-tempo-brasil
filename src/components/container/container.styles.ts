import { StyleSheet } from "react-native";

const { container, ...styles } = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});

export { styles };

export default container;
