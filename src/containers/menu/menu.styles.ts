import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  spacing: {
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
  },
  button: {
    margin: 8,
  },
});

export default styles;
