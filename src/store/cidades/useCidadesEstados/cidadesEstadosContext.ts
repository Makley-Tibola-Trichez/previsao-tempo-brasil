import { createContext } from "react";
import CidadesEstadosType from "../cidadesEstadosSlice.types";

const CidadesEstadosContext = createContext({} as CidadesEstadosType);

export default CidadesEstadosContext;
